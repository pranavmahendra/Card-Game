using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CardDeck : MonoBehaviour
{
    private List<Card> deckOfCards;   // Contains 52 cards.

    private void Start()
    {
        deckOfCards = new List<Card>();

        for (int i = 0; i < 13; i++)
        {
            
        }
    }
}
